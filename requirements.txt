fastapi
uvicorn
sqlalchemy
psycopg2-binary
reportlab
pillow
python-dotenv
python-multipart
zeroconf
slowapi[redis]
bcrypt
pyjwt
email-validator
# Protección CSRF para endpoints de escritura
fastapi-csrf-protect==1.0.7
# Dependencias actualizadas para cerrar CVEs
Werkzeug==3.1.7  # Cierra CVE-2026-27199, CVE-2025-66221, CVE-2026-21860
Flask==3.1.3  # Cierra CVE-2026-27205
ecdsa==0.19.2  # Cierra CVE-2024-23342
safety==3.7.0  # Auditoría continua de vulnerabilidades
# Parsing de configuración declarativa
PyYAML>=6.0.1
# Caché con Redis
redis==4.6.0  # Cliente Redis para caché (compatible con fastapi-cache2)
fastapi-cache2[redis]==0.2.2  # Caché con soporte Redis
# Azure Key Vault para gestión de secretos
azure-identity
azure-keyvault-secrets
# Cifrado AES-256-GCM para PII y derivación de claves HKDF-SHA256
cryptography==42.0.8
# Procesamiento asíncrono con Celery
celery[redis]
# Migraciones de base de datos
alembic
# Validación y sanitización de entrada
bleach
python-magic
# Herramientas de calidad de código
mypy
ruff
pre-commit
# Servidor de producción
gunicorn
requests  # Para health checks
# Testing con Redis mock
fakeredis[aioredis]>=2.0.0  # Mock de Redis para tests sin infraestructura real
# Envío asíncrono de emails para alertas de seguridad
aiosmtplib==3.0.1
# Cliente HTTP asíncrono para webhooks de alertas de seguridad
aiohttp==3.9.5
