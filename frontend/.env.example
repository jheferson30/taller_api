# Copia este archivo como .env y ajusta los valores

# Debe ser igual a ADMIN_PASSWORD del backend
VITE_ADMIN_PASSWORD=cambia_esta_contrasena

# URL del backend (dejar vacio si el frontend lo sirve el mismo servidor)
VITE_API_URL=
